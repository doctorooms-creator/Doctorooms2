import BedTransferClient from '@/app/dashboard/hospital/bed-transfer/client'

export const metadata = { title: 'Bed Transfer — Receptionist' }

export default function ReceptionistBedTransferPage() {
  return <BedTransferClient />
}
