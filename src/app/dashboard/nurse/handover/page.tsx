import HandoverClient from './client'

export const metadata = {
  title: 'Shift Handover | Nurse Dashboard',
  description: 'Nurse shift handover management — incoming and outgoing',
}

export default function HandoverPage() {
  return <HandoverClient />
}
